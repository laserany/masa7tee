import React from 'react'
import Button from 'react-bootstrap/Button'

const LanguageButton = ({ children }) => {
  return <Button variant='outline-dark'>{children}</Button>
}

export default LanguageButton
